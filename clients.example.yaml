clients:
  - name: Example Client
    account_id: "123456789"
    company_id: "987654321"
    # Optional: per-client Airtable overrides (fallback to env/default flags)
    airtable_base_id: "appXXXXXXXXXXXXXX"
    airtable_table_name: "Call Transcripts"
    # Required for eval-airtable: per-client agent prompt directory.
    # Expected files: gatekeeper.md, evaluator.md, parser.md
    prompt_dir: "agents/ExampleClient"
